import 'package:flutter/material.dart';

class CelebView extends StatelessWidget {
  const CelebView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('celeb view')],
    );
  }
}
